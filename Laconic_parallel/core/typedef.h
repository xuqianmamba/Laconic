#ifndef TYPEDEF_H
#define TYPEDEF_H
#include "atomic.h"

// typedef std::atomic<std::pair<int, int>> concurrent_pair;
// typedef std::vector<std::atomic<std::pair<int, int>>> concurrent_array;

#endif  // TYPEDEF_H